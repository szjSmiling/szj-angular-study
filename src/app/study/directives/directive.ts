import { HighlightDirective } from './highlight.directive';
import { UnlessDirective } from './unless.directive';
const directives = [
  HighlightDirective,
  UnlessDirective,
]

export { directives }